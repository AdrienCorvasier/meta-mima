# Tag /dev/esc0 so systemd-udevd generates dev-esc0.device; escd.service
# orders itself after that unit (After=/Wants=dev-esc0.device) and would
# otherwise never start, since custom character-device classes aren't
# tagged for systemd by udev's built-in rules.
SUBSYSTEM=="esc", TAG+="systemd"
